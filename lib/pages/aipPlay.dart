import 'package:flutter/material.dart';

class AipPlay extends StatelessWidget {
  const AipPlay({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("AipPlay"),
      ),
      body: Center(child: Text('AipPlay'),),
    );
  }
}